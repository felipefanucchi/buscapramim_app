import {StyleSheet} from 'react-native'

export default StyleSheet.create({
	container: {
		position: 'absolute',
		bottom: 40,
		justifyContent: 'center',
		backgroundColor: 'red',
		width: 300,
		height: 45,
		zIndex: 999
	}
});